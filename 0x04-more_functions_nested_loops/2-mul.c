#include "main.h"
/**
* _mul - returns value of the multiplication of the numbers
* @a: input to be multiplied
* @b: input to be mulitplied
* Return: int.
*/
int _mul(int a, int b)
{
	return (a * b);
}
