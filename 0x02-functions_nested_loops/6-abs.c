#include<main.h>
/**
 * 6-abs - function that computes the absolute value
 * @n: The input number to check
 * Return: int.
 */
int _abs(int a)
{
	if(a > 0)
	{
	return (a);
	}
	else
	{
	return (-1 * a);
	}
}
