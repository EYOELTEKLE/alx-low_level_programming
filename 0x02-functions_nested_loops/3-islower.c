#include "main.h"
/**
 * _islower - function that prints the alphabet, lowercase
 * You can only use _putchar twice in your code
 * Return: integer.
 */
int _islower(int c)
{
	if(c >= 97 && c <= 177)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
