#include "main.h"
/**
 * _abs - function that computes the absolute value
 * @a: The input number to check
 * Return: int.
 */
int _abs(int a)
{
	if (a > 0)
	{
	return (a);
	}
	else
	{
	return (-1 * a);
	}
}
